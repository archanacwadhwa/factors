#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int accept(char *name)
{
    printf("\n enter the name: ");
    gets(name);
    printf("\n the name is :");
    puts(name);
}
int main()
{
    char *name;
    name=(char *)malloc(sizeof(char));
    accept(name);
    return 0;
}
